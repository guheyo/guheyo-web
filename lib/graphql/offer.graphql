fragment offer on OfferResponse {
  id
  createdAt
  updatedAt
  name
  description
  price
  priceCurrency
  businessFunction
  status
  source
  images {
    ...image
  }
  guildId
  productCategoryId
  seller {
    ...seller
  }
  brandId
}

query findOffers(
  $productCategoryId: ID!
  $cursor: ID
  $skip: Int!
  $take: Int!
) {
  findOffers(
    productCategoryId: $productCategoryId
    cursor: $cursor
    skip: $skip
    take: $take
  ) {
    edges {
      node {
        ...offer
      }
      cursor
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}

query findOfferById(
  $id: ID!
) {
  findOfferById(
    id: $id
  ) {
    ...offer
  }
}
