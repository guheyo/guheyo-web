mutation RefreshTokens {
  refreshTokens {
    accessToken
    refreshToken
  }
}

mutation ReGenerateTokens {
  reGenerateTokens {
    accessToken
    refreshToken
  }
}

mutation Logout {
  logout {
    provider
    socialId
  }
}
