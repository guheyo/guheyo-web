import tailwindConfig from '@/tailwind.config';

const {
  theme: { colors },
} = tailwindConfig;

export const DEFAULT_INPUT_TEXT_COLOR = colors['light-200'];

export const DEFAULT_INPUT_TEXT_BACKGROUND_COLOR = colors['dark-400'];
