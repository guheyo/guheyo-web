'use client'

import React, {memo} from 'react'

const UserMenu = () => {
  const options = [
    {value: 'mypage', label: '내 페이지'},
    {value: 'keyword', label: '키워드 알림'},
    {value: 'logout', label: '로그아웃'},
  ]

  return <></>
}

export default memo(UserMenu)
