export async function generateMetadata() {
  return {
    title: `거래 후기 검색 | 구해요`,
    Description: `거래 후기를 찾아보세요`,
  };
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
