export async function generateMetadata() {
  return {
    title: `교환글 검색 | 구해요`,
    Description: `교환글을 찾아보세요`,
  };
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
